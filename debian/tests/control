# upstream packaging shipped dh-rust/librust-* crate tests;
# this fork builds online against crates.io and ships only the CLI,
# so exercise the binary directly instead
Test-Command: wasm-tools --version && echo '(module)' | wasm-tools parse - | wasm-tools validate && echo '(module)' | wasm-tools parse | wasm-tools print
Depends: wasm-tools
